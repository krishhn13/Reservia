<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Reservia - Payment</title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Custom CSS -->
  <%- include("includes/styles.ejs") %>

  <!-- Inline fix to force navbar link color -->
  <style>
    .navbar-link {
      color: white !important;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
    }
    .navbar-link:hover {
      color: #dacf8e !important; /* golden hover */
    }
  </style>
</head>

<body class="min-h-screen flex flex-col bg-gradient-to-br from-[#c2b280] via-[#e0cda9] to-[#f5f5dc] text-[#4b3c2a]">

  <!-- Header Include with forced white text -->
  <header class="text-white">
    <%- include("includes/header.ejs") %>
  </header>

  <!-- Main Payment Section -->
  <main class="flex-grow flex items-center justify-center p-6">
    <div class="bg-white shadow-2xl rounded-3xl p-10 max-w-2xl w-full border border-[#d1bfa3]">
      <h2 class="text-4xl font-extrabold text-center mb-8 text-[#4b3c2a]">Reservia Payment</h2>

      <form class="space-y-6" method="get" action="/process-payment">
        <div>
          <label for="card-name" class="block font-semibold text-lg mb-2">Cardholder Name</label>
          <input id="card-name" name="cardName" type="text" required placeholder="John Doe" class="w-full px-6 py-3 border rounded-2xl text-lg focus:outline-none focus:ring-2 focus:ring-[#8b5e3c]" />
        </div>

        <div>
          <label for="card-number" class="block font-semibold text-lg mb-2">Card Number</label>
          <input id="card-number" name="cardNumber" type="text" required placeholder="1234 5678 9012 3456" class="w-full px-6 py-3 border rounded-2xl text-lg focus:outline-none focus:ring-2 focus:ring-[#8b5e3c]" />
        </div>

        <div class="flex space-x-6">
          <div class="flex-1">
            <label for="expiry" class="block font-semibold text-lg mb-2">Expiry</label>
            <input id="expiry" name="expiry" type="text" required placeholder="MM/YY" class="w-full px-6 py-3 border rounded-2xl text-lg focus:outline-none focus:ring-2 focus:ring-[#8b5e3c]" />
          </div>
          <div class="flex-1">
            <label for="cvc" class="block font-semibold text-lg mb-2">CVC</label>
            <input id="cvc" name="cvc" type="text" required placeholder="123" class="w-full px-6 py-3 border rounded-2xl text-lg focus:outline-none focus:ring-2 focus:ring-[#8b5e3c]" />
          </div>
        </div>

        <button type="submit" class="w-full bg-[#8b5e3c] hover:bg-[#6e472e] text-white text-lg font-bold py-3 px-6 rounded-2xl transition duration-300">
          Proceed to Payment
        </button>
      </form>

      <p class="text-sm text-gray-600 text-center mt-8">
        Your payment is securely processed and end-to-end encrypted. We never store your card data.
      </p>
    </div>
  </main>

  <!-- Footer Include -->
  <footer class="mt-auto">
    <%- include("includes/footer.ejs") %>
  </footer>

</body>
</html>
