<style>
    /* Basic styling */
    body {
        font-family: 'DM Sans', sans-serif; /* Assuming this is a font you're using */
        margin: 0;
        background-color: #000 !important; /* Setting background to black with !important */
        color: #fff; /* Assuming white text for better contrast on black */
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .header {
        background-color: #000000; /* Darker background for header */
        width: 100%;
        padding: 15px 20px;
        box-sizing: border-box;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .logo {
        max-width: 160px;
        height: auto;
    }
    .navbar-list {
        display: flex;
        list-style: none;
        padding: 0;
        margin: 0;
        gap: 20px;
    }
    .navbar-link {
        color: #fff;
        text-decoration: none;
        position: relative; /* For hover underline */
        padding-bottom: 5px; /* Space for underline */
    }
    .hover-underline::after {
        content: '';
        position: absolute;
        left: 0;
        bottom: 0;
        width: 0%;
        height: 2px;
        background-color: #5cb85c; /* Example accent color */
        transition: width 0.3s ease;
    }
    .hover-underline:hover::after {
        width: 100%;
    }
    .separator {
        display: none; /* You might have styling for this in your CSS */
    }
    .logo-container {
        text-align: center;
        margin-top: 20px;
        margin-bottom: 20px;
    }
    .orders-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
        width: 90%;
        max-width: 1200px;
        margin-bottom: 20px;
    }
    .order-container {
        background-color: #000000; /* Darker background for order container */
        padding: 15px;
        border-radius: 8px;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
        border-left: 3px solid #5cb85c;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        min-height: 150px;
        color: #eee; /* Light text for order details */
    }
    .order-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
        color: #ccc;
        font-size: 0.9em;
    }
    .order-id {
        font-weight: bold;
    }
    .order-date {
        font-size: 0.8em;
    }
    .order-details {
        margin-top: 8px;
        padding-left: 10px;
        font-size: 0.85em;
    }
    .order-item {
        margin-bottom: 3px;
    }
    .order-total-status {
        margin-top: 10px;
        font-size: 0.9em;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .empty-message {
        text-align: center;
        color: #777;
        font-style: italic;
    }
    .delete-reservation-btn {
        background-color: #dc3545; /* Red color for delete button */
        color: white;
        border: none;
        padding: 8px 12px;
        border-radius: 5px;
        cursor: pointer;
        font-size: 0.9em;
        margin-top: 10px;
        align-self: flex-start; /* Align to the left */
    }
    .delete-reservation-btn:hover {
        background-color: #c82333;
    }
    /* You might need to include or link your styles.css for full styling */
</style>